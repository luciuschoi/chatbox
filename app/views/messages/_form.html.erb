<%= simple_form_for(@message, remote: true) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :content, label: false, placeholder: "입력하세요." %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
